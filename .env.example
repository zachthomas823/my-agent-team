# AWS credentials are NOT configured here.
# Run scripts/launch-agents.sh — it resolves STS credentials on the host
# and generates docker/settings.json which is mounted into agent containers.
#
# Optional: override the default AWS profile (default: nfl-dm-api-dev)
# AWS_PROFILE=nfl-dm-api-dev
